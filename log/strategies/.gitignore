# Ignore all strategy log files
*.log

# Ignore runtime state/ledger files (regenerated on every run)
*.json

# Keep this .gitignore file
!.gitignore

# This ensures the log/strategies folder exists in git but log files/state are not tracked